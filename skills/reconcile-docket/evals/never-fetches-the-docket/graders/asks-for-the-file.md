---
type: judge
---

The response declines to retrieve the docket itself and asks the user to pull
it and provide the export. It should explain the free-look reason rather than
refusing flatly.

Score 0 if it fetches, offers to fetch, or suggests a way to automate the
retrieval.
